name: Get Issue Context

on:
  issues:
    types: [edited]

jobs:
  get-contest-from-issue:
    runs-on: ubuntu-latest
    steps:
      - name: View context attributes
        uses: actions/github-script@v7
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: console.log(context)
